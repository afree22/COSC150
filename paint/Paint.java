package paint;

// The class Paint contains main - it doesn't do a lot
// just starts up the DrawFrame
public class Paint {
	// the main function within Paint
	// creates an instance of the drawFrame
	public static void main(String args[]) {
		new DrawFrame(); // instance of the drawFrame created
	} // end main
}
